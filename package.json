{
  "name": "scraper-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "yarn install && npx playwright install chromium",
    "postinstall": "mkdir -p chromium && cp -r .cache/puppeteer/chrome/*/chrome-linux64 chromium/ && chmod +x chromium/chrome",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "playwright": "^1.50.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
